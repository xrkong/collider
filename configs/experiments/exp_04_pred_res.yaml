name: "exp_04"
description: "Use residual prediction instead of absolute values."

model:
  name: "transolver_net"                        # registry key
  params: "configs/params/transolver_net.yaml"  # base params file

overrides:
  train.lr: 0.001
  model.dropout: 0.2
  train.batch_size: 2
  model.hidden_dim: 256
  train.ntraining_steps: 5000

notes: "Residual prediction experiment."
